<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lingayatpanchasanagam.sbjm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LingayatPanchasangamActivity"
            android:label="@string/title_activity_lingayat_panchasangam"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListOfMembersActivity" />
        <activity
            android:name=".LingayatPanchamsangamScreenActivity"
            android:label=""
            android:theme="@style/AppThemeNew" />
        <activity
            android:name=".LogInActivity"
            android:label=""
            android:theme="@style/AppThemeNew"></activity>
    </application>

</manifest>